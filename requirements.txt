# Prompter - dependencias (Python 3.12). No Windows instale tambem pywin32; torch CPU:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
aiohttp>=3.10
mido>=1.3
python-rtmidi>=1.5
faster-whisper>=1.2
ctranslate2>=4.5
demucs>=4.1
torch>=2.2   # macOS Intel para em 2.2.x (numpy<2 nesse caso)
av>=13
soundfile>=0.12
numpy
pystray>=0.19
pillow>=10
py-ptsl>=500
grpcio>=1.60
huggingface_hub
pywin32; sys_platform == "win32"
